tokenSecretKey=exampleKey
tokenLife=1d

refreshTokenSecret=exampleKey
refreshTokenLife=30d

# If commented out, the url will be generated from the rest values
# db=mongodb://user:pass@ip:port/grade?authSource=admin
mongoUser=user
mongoPassword=password
mongoDatabase=db
mongoHost=ip:port

flask=flaskUrl
